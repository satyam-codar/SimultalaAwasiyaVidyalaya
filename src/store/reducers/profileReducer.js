/** @format */
import { initstate } from "./Batch10";
import { initstate11 } from "./Batch11";
import { initstate12 } from "./Batch12";
import { initstate13 } from "./Batch13";
const profileReducer = (state = initstate, action) => {
	return state;
};

export default profileReducer;

export const profileReducerFor11 = (state = initstate11, action) => {
	return state;
};

export const profileReducerFor12 = (state = initstate12, action) => {
	return state;
};

export const profileReducerFor13 = (state = initstate13, action) => {
	return state;
};

// export default profileReducerFor11;
